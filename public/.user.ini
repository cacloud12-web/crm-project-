; Hostinger / shared hosting: PHP-FPM picks this up from public/ when allowed.
; Keep above GOOGLE_DOCUMENT_AI_MAX_FILE_MB / online batch limits (app allows up to ~100MB+).
upload_max_filesize = 128M
post_max_size = 128M
memory_limit = 512M
max_execution_time = 300
