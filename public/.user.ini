; Hostinger / shared hosting: place in public/ so PHP-FPM picks up local overrides when allowed.
; Values must exceed GOOGLE_DOCUMENT_AI_MAX_FILE_MB (default 20).
upload_max_filesize = 25M
post_max_size = 30M
